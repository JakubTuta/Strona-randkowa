# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on:
  pull_request:
    branches:
      - release
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "API_KEY=${{ secrets.API_KEY }}" > .env
          echo "AUTH_DOMAIN=strona-randkowa.firebaseapp.com" >> .env
          echo "PROJECT_ID=strona-randkowa" >> .env
          echo "STORAGE_BUCKET=strona-randkowa.appspot.com" >> .env
          echo "MESSAGING_SENDER_ID=13507317281" >> .env
          echo "APP_ID=1:13507317281:web:722835674189d736221d6" >> .env
      - run: npm install
      - run: npm run generate
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_STRONA_RANDKOWA }}
          projectId: strona-randkowa